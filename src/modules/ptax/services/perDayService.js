const quotePerDay = async (url, options) => {
    // try {
    //     const res = await fetch(url, options);
    //     const json = await res.json();
    // } catch (e) {
    // } finally {
    //     if (!signal.aborted) {
    //         setCurrenciesLoading(false);
    //     }
    // }
};

export {
    quotePerDay
}